#!/bin/bash
# Example after_install.sh

# Move files to the deployment directory (if needed)
# mv /some/temporary/location/* /path/to/deployment/

# Set file permissions
chmod +x /home/ubuntu/app-content/myapp
